#include <stdio.h>
#include <stdlib.h>
int main() {
	int count=0;    //记录出现9的次数
	for (int n = 1; n <= 100;n++) {
		if (n%10==9) {   //个位数，满足条件，次数加一
			count++;
		}
		if (n/10 == 9){  //十位数，满足条件，次数加一
			count++;
		}
	}
	printf("%d\n", count);
	system("pause");
	return 0;
}
