#include<stdio.h>
#include<stdlib.h>
int main()
{
 int a[10];
 int i, max;
 for (i = 0; i <= 9; i++)
 {
  scanf_s("%d", &a[i]);
 }
     max = a[0];
for (i = 0; i <= 9; i++)
 {
  if (max < a[i])
   max = a[i];
 }
  printf("最大值为%d", max);
 system("pause");
 return 0;
}
