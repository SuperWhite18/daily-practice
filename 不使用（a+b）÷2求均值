#include <stdio.h>
#include <stdlib.h>
#pragma warning(disable:4996)
int main()
{
	int num1 = 0;
	int num2 = 0;;
	printf("Please Enter Number:");
	scanf("%d%d", &num1, &num2);
	int avg = num1 - (num1 - num2) / 2;
	printf("result=%d", avg);
	system("pause");
	return 0;
}
